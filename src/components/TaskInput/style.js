import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    inputW: {
       flexDirection: 'row',
       alignItems: 'center', 
       marginBottom: 20,
    },
    ipt: {
        height: 40,
        borderWidth: 1,
        padding: 10,
        color: '#000',
        borderColor: '#fff'
    }
})

export default styles