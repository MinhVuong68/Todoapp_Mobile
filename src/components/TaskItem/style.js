import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    checkboxContainer: {
        flexDirection: "row",
        height: 36,
        marginBottom: 20,
        alignItems: 'center',
        backgroundColor: '#fff',
    },
    checkbox: {
        alignSelf: "center",
    },
})

export default styles